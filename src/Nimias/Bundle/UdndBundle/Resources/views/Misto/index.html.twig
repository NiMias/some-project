{% extends '::base.html.twig' %}

{% block body -%}
    <h1>Misto list</h1>

    <table class="records_list">
        <thead>
            <tr>
                <th>Oblastid</th>
                <th>Rayonid</th>
                <th>Name</th>
                <th>Id</th>
                <th>Actions</th>
            </tr>
        </thead>
        <tbody>
        {% for entity in entities %}
            <tr>
                <td><a href="{{ path('misto_show', { 'id': entity.id }) }}">{{ entity.oblastId }}</a></td>
                <td>{{ entity.rayonId }}</td>
                <td>{{ entity.name }}</td>
                <td>{{ entity.id }}</td>
                <td>
                <ul>
                    <li>
                        <a href="{{ path('misto_show', { 'id': entity.id }) }}">show</a>
                    </li>
                </ul>
                </td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

    {% endblock %}
